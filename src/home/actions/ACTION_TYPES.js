import { createActionSet } from '../../util/action.helpers';

export const MENU_ITEMS     = createActionSet('MENU_ITEMS');

export const BRAND_FILTER     = createActionSet('BRAND_FILTER');

export const SEARCH     = createActionSet('SEARCH');